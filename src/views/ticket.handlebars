<head>
  <style>
    table { border-collapse: collapse; width: 100%; max-width: 600px; margin: 0
    auto; } th, td { border: 1px solid #dddddd; text-align: left; padding: 8px;
    } th { background-color: #f2f2f2; }
  </style>
</head>
<body>
  <div>
    <h1>Pedido confirmado</h1>
    <p>¡Gracias por tu compra! Aquí tienes los detalles de tu pedido:</p>
    <table>
      <thead>
        <tr>
          <th>Producto</th>
          <th>Cantidad</th>
          <th>Total</th>
        </tr>
      </thead>
      <tbody>
        {{#each products}}
          <tr>
            <td>{{lookup this "title"}}</td>
            <td>{{lookup this "quantity"}}</td>
            <td>${{lookup this "subtotal"}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>

    <p>Total a pagar: ${{lookup ticket "amount"}}</p>
    <p>¡Gracias por tu compra!</p>
    <a href="/products"><button class="home-button">Home</button></a>

  </div>
</body>